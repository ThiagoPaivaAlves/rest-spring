#Authentication Config Stuff
security:
  jwt:
    token:
      secret-key: 53cr7
      expire-length: 3600000

#General Configuration
spring:
  application:
    name: rest-spring
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_spring_udemy?useTimezone=true&serverTimezone=UTC
    username: root
    password: ""
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    show-sql: false
  flyway:
    enabled: true