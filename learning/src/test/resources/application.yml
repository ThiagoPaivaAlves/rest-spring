security:
  jwt:
    token:
      secret-key: 53cr7
      expire-length: 3600000 #in milliseconds
      refresh-length: 3 #in hours

server:
  port: 8888
spring:
  application:
    name: rest-spring
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
  flyway:
    enabled: true